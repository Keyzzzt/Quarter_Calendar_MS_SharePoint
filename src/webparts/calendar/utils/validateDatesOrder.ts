import {parseDateString} from './parseDateString'// Validates date that user has selected when create/update task. Start date should be earlier that end date.export const validateDatesOrder = (startDate: string, endDate: string): boolean => {    const {startYear, startMonth, startDay, endYear, endMonth, endDay} = parseDateString(startDate, endDate)    if (startYear > endYear) return false    if (startYear === endYear && startMonth > endMonth) return false    if (startYear === endYear && startMonth === endMonth && startDay > endDay) return false    return true}